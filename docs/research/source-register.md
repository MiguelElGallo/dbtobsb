# Research source register

- Last refreshed: 2026-07-18
- Policy: Prefer current official product documentation and first-party repositories. Re-check time-sensitive feature status before implementing or publishing a claim.

## Documentation baseline

| Source | Decision supported | Current caution |
|---|---|---|
| [Zensical repository](https://github.com/zensical/zensical), [project setup](https://zensical.org/docs/setup/basics/), [navigation](https://zensical.org/docs/setup/navigation/), and [publishing](https://zensical.org/docs/publish-your-site/) | Use `zensical.toml`, a dedicated reader-only `docs_dir`, explicit navigation, local search, a strict build, and a pinned GitHub Pages workflow | Keep local and CI builds on the same exact Zensical version and scan the generated Pages artifact before treating publication as complete |
| [Diátaxis](https://diataxis.fr/), [tutorials](https://diataxis.fr/tutorials/), [how-to guides](https://diataxis.fr/how-to-guides/), [reference](https://diataxis.fr/reference/), and [explanation](https://diataxis.fr/explanation/) | Make the four reader needs the top-level site structure and keep each page focused on one need | Production installation is a task guide, not a choice-free learning tutorial; internal evidence and plans stay outside customer navigation |
| [FastAPI Python types tutorial](https://fastapi.tiangolo.com/python-types/) | Use short sections, small runnable examples, expected output, progressive disclosure, plain definitions, and direct language | Style is a clarity reference only; dbtobsb does not copy FastAPI product claims or examples |

## Databricks product baseline

| Source | Decision supported | Current caution |
|---|---|---|
| [Databricks Free Edition](https://learn.microsoft.com/en-us/azure/databricks/getting-started/free-edition) and [Free Edition limitations](https://learn.microsoft.com/en-us/azure/databricks/getting-started/free-edition-limitations) | The official current personal-use product name is Free Edition; it replaced Community Edition in 2025. Free Edition is serverless-only, quota-limited, non-commercial, and lacks the account, compliance, networking, support, and SLA boundary expected by this release | Individual Apps, Jobs, or SQL features can exist in Free Edition; do not imply each component is absent. The complete dbtobsb v0.3 contract is explicitly unsupported there |
| [Azure Databricks per-workspace URLs](https://learn.microsoft.com/en-us/azure/databricks/workspace/per-workspace-urls) | The supported Azure workspace has a canonical `adb-<workspace-id>.<number>.azuredatabricks.net` URL | The v0.3 installer filters for Azure and validates this canonical host shape. Free Edition remains a separate attended support prohibition because the official workspace APIs do not document a stable edition detector for this installer |
| [Azure Databricks Apps overview](https://learn.microsoft.com/en-us/azure/databricks/dev-tools/databricks-apps/) | Private App runs inside the workspace; Premium and region/compliance prerequisites; App compute lifecycle | Running Apps are billed; compliance-profile workspaces require admin enablement |
| [Declarative Automation Bundles overview](https://learn.microsoft.com/en-us/azure/databricks/dev-tools/bundles/) | Source-controlled App, Job, permissions, and CI/CD baseline | Pin and validate the CLI; use plain YAML for the regulated baseline |
| [Databricks CLI release types](https://learn.microsoft.com/en-us/azure/databricks/dev-tools/cli/) | CLI 1.0.0 and above is GA | Product still pins and requalifies one exact deployment-only version |
| [Databricks CLI 1.7.0 release](https://github.com/databricks/cli/releases/tag/v1.7.0) and [official checksum guidance](https://learn.microsoft.com/en-us/azure/databricks/dev-tools/cli/install#source-installation-for-linux-macos-and-windows) | Exact GA deployment-tool candidate at commit `2f68ee4951ef96fa9d99e40c8ebadccf08412d58`; the release publishes per-platform archives and `SHA256SUMS` | Bundle and verify the platform-specific release asset and extracted executable before first execution; do not select an unknown binary from `PATH` or accept a future patch/minor implicitly |
| [Bundle authentication](https://learn.microsoft.com/en-us/azure/databricks/dev-tools/bundles/authentication) | Databricks recommends OAuth U2M for attended Bundle authentication | Explicitly confirm profile, canonical host, and current identity before mutation |
| [OAuth U2M authorization](https://learn.microsoft.com/en-us/azure/databricks/dev-tools/auth/oauth-u2m) | Each attended actor can use a profile in that actor's own OS account through unified authentication | Access tokens are refreshable local credential state, not privilege elevation or proof of the physical human; profiles/tokens never cross OS accounts |
| [Databricks CLI authentication](https://learn.microsoft.com/en-us/azure/databricks/dev-tools/cli/authentication) | OAuth U2M and native secure storage are the attended baseline | Force `DATABRICKS_AUTH_STORAGE=secure`; ordinary login may otherwise fall back to plaintext storage |
| [Databricks CLI auth commands](https://learn.microsoft.com/en-us/azure/databricks/dev-tools/cli/reference/auth-commands) | Login/logout and token-output behavior define prohibited production fallbacks and development-only diagnostics | The production bootstrap never logs out, deletes an actor-owned profile, calls `auth token`/`auth env`, or depends on `auth describe`/Current User Preview. The development-only P0 smoke retains its narrower documented token-output exception. |
| [CLI 1.7.0 in-process U2M credentials](https://github.com/databricks/cli/blob/2f68ee4951ef96fa9d99e40c8ebadccf08412d58/libs/auth/credentials.go#L55-L137) and [native storage resolver](https://github.com/databricks/cli/blob/2f68ee4951ef96fa9d99e40c8ebadccf08412d58/libs/auth/storage/cache.go#L31-L103) | The signed local bridge can read OAuth U2M tokens directly from the CLI's OS-native store instead of shelling out to a token-output command | Force the explicit secure mode so read fallback cannot select the plaintext cache; keep the bearer inside the fixed-operation helper and reject every non-`databricks-cli` profile or host mismatch before a workspace request |
| [`session_user()` SQL function](https://learn.microsoft.com/en-us/azure/databricks/sql/language-manual/functions/session_user) and [`current_user()` compatibility alias](https://learn.microsoft.com/en-us/azure/databricks/sql/language-manual/functions/current_user) | One fixed read-only `session_user()` Statement Execution query on the dedicated installer warehouse binds the exact in-process bearer to the approved actor without a Preview identity API | Current documentation recommends `SESSION_USER` for Databricks SQL/Runtime 14.1 and above and retains `current_user` only as an alias. Compare only the approved actor fingerprint in protected local state; raw identity is request-local Personal Data and is never written to receipts, logs, URLs, screenshots, or handoff state |
| [Azure workspace URL and ID format](https://learn.microsoft.com/en-us/azure/databricks/workspace/workspace-details) | The supported per-workspace host is `adb-<workspace-id>.<random-number>.azuredatabricks.net`, with the numeric workspace ID immediately after `adb-` | Reject legacy regional hosts and derive the ID only after the native bridge proves the exact signed canonical host/profile/actor; CLI 1.7.0 Bundle JSON does not itself expose `workspace.host` or `workspace_id` in the qualified Azure target |
| [Databricks configuration profiles](https://learn.microsoft.com/en-us/azure/databricks/dev-tools/auth/config-profiles) | Explicit complete profiles isolate each actor's configuration inside their managed OS account | Reject `DEFAULT`, incomplete/ambiguous profiles, injected auth environment, and ad hoc profile copying; an SDK call receives explicit profile and host rather than assuming CLI precedence |
| [Databricks CLI troubleshooting](https://learn.microsoft.com/en-us/azure/databricks/dev-tools/cli/troubleshooting) | Secure token-store and plaintext-fallback preflight | Regulated path fails closed when OS-native secure storage cannot be verified |
| [Bundle configuration reference](https://learn.microsoft.com/en-us/azure/databricks/dev-tools/bundles/reference) | Exact `bundle.databricks_cli_version` constraint | Requalify every CLI update rather than accepting a future minor automatically |
| [Databricks CLI 1.3.0 release](https://github.com/databricks/cli/releases/tag/v1.3.0) | Direct engine became GA and default for new deployments | Pin `bundle.engine: direct`; never rely on a changing implicit default |
| [Bundle direct deployment engine](https://learn.microsoft.com/en-us/azure/databricks/dev-tools/bundles/direct) | GA Direct engine is the new-product baseline and Terraform is headed for deprecation | Qualify separate local/remote state, plan/diff, unsupported-field, and documented drift behavior |
| [CLI 1.7.0 saved-plan deploy](https://github.com/databricks/cli/blob/2f68ee4951ef96fa9d99e40c8ebadccf08412d58/cmd/bundle/deploy.go#L27-L44) | Apply the exact approved Direct JSON plan with `bundle deploy --plan` | The wrapper must additionally bind CLI, identity, variables, source/build hashes, and a fresh semantic-plan comparison |
| [CLI 1.7.0 selected plan](https://github.com/databricks/cli/blob/2f68ee4951ef96fa9d99e40c8ebadccf08412d58/cmd/bundle/plan.go#L30-L55) | `bundle plan --select` can isolate supported runtime resources for focused review | Bind the complete resource-selection set in the sidecar and apply only the resulting saved plan; no selection may add a migration Job/service principal, schema/grants node, DDL, or receipt writer |
| [CLI 1.7.0 saved-plan validation](https://github.com/databricks/cli/blob/2f68ee4951ef96fa9d99e40c8ebadccf08412d58/cmd/bundle/utils/process.go#L240-L265) and [`ValidatePlanAgainstState`](https://github.com/databricks/cli/blob/2f68ee4951ef96fa9d99e40c8ebadccf08412d58/bundle/direct/bundle_plan.go#L42-L57) | Direct Apply requires the saved plan lineage/serial to match current Direct state; any successful Apply makes a plan captured from the former serial stale | Never precompute executable shell, bound-unshared, and user-access-only plans. Generate/review/apply each from fresh state in order and never bypass serial validation; initial empty lineage also needs wrapper safeguards |
| [CLI 1.7.0 Direct plan-time remote read](https://github.com/databricks/cli/blob/2f68ee4951ef96fa9d99e40c8ebadccf08412d58/bundle/direct/bundle_plan.go#L207-L318) and [saved-plan state loading](https://github.com/databricks/cli/blob/2f68ee4951ef96fa9d99e40c8ebadccf08412d58/bundle/direct/bundle_plan.go#L62-L115) | Direct reads remote state while planning and carries that typed snapshot in the saved plan used for apply | Bound-unshared planning occurs only after shell Apply; user-access planning occurs only after deployment reconciliation and stop. Regenerate on drift. Lineage/serial protects Direct state, not later external U2M privilege changes; audit live direct/effective grants too |
| [CLI 1.7.0 Direct App create adapter](https://github.com/databricks/cli/blob/2f68ee4951ef96fa9d99e40c8ebadccf08412d58/bundle/direct/dresources/app.go#L96-L144) | A first-install plan without state calculates App creation and the adapter rejects an already-existing non-deleting App | Two initial plans calculated before either Apply are not composable: the second is another create, not a final update |
| [CLI 1.7.0 Direct grants adapter](https://github.com/databricks/cli/blob/2f68ee4951ef96fa9d99e40c8ebadccf08412d58/bundle/direct/dresources/grants.go#L120-L193) | The adapter identifies every plan-time remote principal omitted from desired assignments and sends `REMOVE ALL_PRIVILEGES` for each | A Direct schema `grants` block is an authoritative destructive list, not an additive privilege mechanism; never use it on the customer-owned product schema or to coexist with App/customer assignments |
| [CLI 1.7.0 Direct deploy phase](https://github.com/databricks/cli/blob/2f68ee4951ef96fa9d99e40c8ebadccf08412d58/bundle/phases/deploy.go#L131-L205) | Deployment still uses current staged/source content and can execute a pre-deploy script before applying the saved plan | Freeze/hash source; prohibit mutation-bearing pre-deploy scripts because their remote effects are outside the Direct diff |
| [CLI 1.7.0 App Bundle runner](https://github.com/databricks/cli/blob/2f68ee4951ef96fa9d99e40c8ebadccf08412d58/bundle/run/app.go#L44-L99) | `bundle run <app-key>` starts App compute before unconditionally requesting a new `SNAPSHOT` deployment; an existing active deployment can therefore run first | Fresh install requires an empty deployment baseline, one wrapper invocation, no wrapper retry, immediate stop/reconciliation on every exit, and disclosure of the bound read-only billed interval. Existing deployments, upgrade, and rollback are unsupported in v1 rather than described as zero-authority |
| [CLI 1.7.0 App deployment helper](https://github.com/databricks/cli/blob/2f68ee4951ef96fa9d99e40c8ebadccf08412d58/bundle/appdeploy/app.go#L80-L112) | After any first deploy-call error, the helper waits for current activity and can issue the deployment POST again; response loss can therefore yield multiple new records | One invocation is not one submission. Stop and fully reconcile the before/after deployment sets after success or error; accept exactly one new matching terminal `SNAPSHOT`, otherwise invalidate and remain stopped |
| [Apps `list-deployments`](https://docs.databricks.com/api/azure/workspace/apps/listdeployments), [Get deployment](https://docs.databricks.com/api/azure/workspace/apps/getdeployment), and [CLI Apps commands](https://learn.microsoft.com/en-us/azure/databricks/dev-tools/cli/reference/apps-commands) | GA deployment list/get surfaces expose IDs, `SNAPSHOT`/`AUTO_SYNC`, `SUCCEEDED`/`FAILED`/`IN_PROGRESS`/`CANCELLED`, and pagination needed for recovery | Consume every page; accept only terminal records and a selected `SUCCEEDED` `SNAPSHOT`; bind lifecycle-free before/after inventories while phase observations separately bind active/pending/compute state; the user never chooses an ID, and zero/multiple/pending/mismatched outcomes fail closed |
| [Apps start/stop commands](https://learn.microsoft.com/en-us/azure/databricks/dev-tools/cli/reference/apps-commands#databricks-apps-start) and [CLI 1.7.0 direct start](https://github.com/databricks/cli/blob/2f68ee4951ef96fa9d99e40c8ebadccf08412d58/cmd/workspace/apps/apps.go#L1519-L1578) | `databricks apps start <name>` starts the last active deployment through the Apps API without deploying code | Start has no deployment-ID precondition; compare exact ID before and after, keep the App setup-only, and stop on mismatch |
| [CLI 1.7.0 Bundle-aware App deploy](https://github.com/databricks/cli/blob/2f68ee4951ef96fa9d99e40c8ebadccf08412d58/cmd/apps/deploy_bundle.go#L69-L137) | No-argument `apps deploy` inside a Bundle project can run enhanced deploy plus App run | Prohibit no-argument/bare `apps deploy` throughout the wrapper; use only the frozen staging invocation and explicit-name get/list/start/stop afterward |
| [CLI 1.7.0 Direct App lifecycle](https://github.com/databricks/cli/blob/2f68ee4951ef96fa9d99e40c8ebadccf08412d58/bundle/direct/dresources/app.go#L96-L240) | Direct App create carries deploy-time `config`, but Update applies resources and permissions without retrofitting that config into an existing deployment; `lifecycle.started:true` can invoke the runner | Create a stopped undeployed shell, then apply a fresh bound-unshared resource/config plan before the sole Bundle-runner deployment so that deployment consumes the mappings. After stop/reconciliation, a fresh user-access plan may change only approved-group `CAN_USE`; keep lifecycle start disabled in every Direct plan |
| [Bundles in air-gapped environments](https://learn.microsoft.com/en-us/azure/databricks/dev-tools/bundles/airgapped-environment) | Pre-stage an exact CLI/container and dependencies | Direct avoids Terraform/HashiCorp inputs; declare only the destinations/assets actually used |
| [Bundle resource reference](https://learn.microsoft.com/en-us/azure/databricks/dev-tools/bundles/resources) | App and Job resources are supported, and App resources can bind existing UC securables | CLI 1.7.0 has no generic managed Delta-table/view resource; the v1 Bundle deliberately omits the customer-owned schema and its grants |
| [Bundle resource permissions](https://learn.microsoft.com/en-us/azure/databricks/dev-tools/bundles/permissions) | Jobs support declarative `IS_OWNER`, `CAN_MANAGE`, `CAN_MANAGE_RUN`, and `CAN_VIEW` ACL entries | Declarative ownership is not an authorization bypass; the applying identity still needs platform permission, and the runtime Bundle deliberately owns no privileged migration Job |
| [CLI 1.7.0 Bundle resource types](https://github.com/databricks/cli/blob/2f68ee4951ef96fa9d99e40c8ebadccf08412d58/bundle/config/resources.go) | Immutable schema confirms supported resources and no tables/views collection | Re-check on every pinned CLI update; do not infer table creation from an App binding |
| [CLI 1.7.0 schema resource](https://github.com/databricks/cli/blob/2f68ee4951ef96fa9d99e40c8ebadccf08412d58/bundle/config/resources/schema.go) | Confirms schema support exists, making its omission an explicit v1 ownership decision rather than a platform limitation | The customer-owned dedicated schema is a typed prerequisite; do not create, adopt, destroy, or attach Direct grants to it |
| [Manage Apps with Bundles](https://learn.microsoft.com/en-us/azure/databricks/dev-tools/bundles/apps-tutorial) | Canonical App-plus-Bundle lifecycle and `bundle run <app-key>` | `bundle deploy` creates/updates the App resource but does not deploy App code to compute |
| [Get started with Apps](https://learn.microsoft.com/en-us/azure/databricks/dev-tools/databricks-apps/get-started) | Successful code deployment exposes a deployment ID, immutable deployment-artifact source path, status, and App start | Resource-only shell/bound/user-access Applies cannot satisfy App-code observation; bind trust to the uniquely reconciled deployment created by the sole bound Bundle-runner invocation |
| [App status and cost](https://learn.microsoft.com/en-us/azure/databricks/dev-tools/databricks-apps/key-concepts#app-status) | Running Apps incur compute cost; stopped Apps are inaccessible and do not incur App-compute cost | A cancellation or stop request is not cost cessation; require bounded stop observation and surface an unverified stop as continuing cost |
| [App state persistence](https://learn.microsoft.com/en-us/azure/databricks/dev-tools/databricks-apps/key-concepts#app-state) | App memory and local files are ephemeral; tables and UC Volumes are persistent | Runtime trust cannot live on App disk or memory |
| [App compute size](https://learn.microsoft.com/en-us/azure/databricks/dev-tools/databricks-apps/compute-size) | `MEDIUM` is the default size and is published at `0.5 DBU/hour`; `LARGE` is `1 DBU/hour` | Pin and read back the approved live size; calculate planned DBUs and successful-stop exposure from the current published rate |
| [App resources](https://learn.microsoft.com/en-us/azure/databricks/dev-tools/databricks-apps/resources) | Dedicated App principal, resource bindings, no hard-coded IDs or PATs | App access and resource authorization remain separate |
| [App authorization](https://learn.microsoft.com/en-us/azure/databricks/dev-tools/databricks-apps/auth) | Shared App-principal authorization is the stable v1 action path | User/on-behalf-of-user authorization is Public Preview; shared identity grants require narrow curated views |
| [App permissions](https://learn.microsoft.com/en-us/azure/databricks/dev-tools/databricks-apps/permissions) | Named App `CAN_USE` and `CAN_MANAGE` groups | App access and runtime resource grants must be reviewed separately |
| [Manage workspace identities](https://learn.microsoft.com/en-us/azure/databricks/admin/users-groups/manage-groups) | Customer-managed named App and role-administrator groups; workspace-domain Account Groups proxy examples use `/api/2.0/account/scim/v2/` | Automatic identity management supports nested groups while the Entra SCIM connector does not; propagation can take minutes. The base product treats every granted group as a whole trusted root and does not claim complete continuous member enumeration |
| [`SHOW GROUPS`](https://learn.microsoft.com/en-us/azure/databricks/sql/language-manual/sql-ref-syntax-aux-show-groups) and [`is_account_group_member`](https://learn.microsoft.com/en-us/azure/databricks/sql/language-manual/functions/is_account_group_member) | An administrator can list direct and indirect parent groups for one named user/group; a session can prove its own direct or indirect account-group eligibility | Use only for the actual migration actors and operator/verifier groups needed to classify current-DML paths. It is not an all-member snapshot or a continuous membership monitor |
| [Workspace Groups API](https://docs.databricks.com/api/azure/workspace/groups) | Confirms a tempting workspace group surface exists | Public Preview; prohibited in the required GA path. Account SCIM/API-invisible automatic-identity cases must not be hidden behind a completeness claim |
| [Identity-management best practices](https://learn.microsoft.com/en-us/azure/databricks/admin/users-groups/best-practices) | Identity federation and account-managed group lifecycle are the enterprise baseline | Product action identity still uses explicit customer-local enrollment, expiry, and revocation |
| [App HTTP headers](https://learn.microsoft.com/en-us/azure/databricks/dev-tools/databricks-apps/http-headers) | Proxy-authenticated `X-Forwarded-User` and `X-Request-Id` are request inputs | The user value is IdP-provided and may be a username/email; never assume it equals an opaque workspace principal ID |
| [CLI 1.7.0 local App headers](https://github.com/databricks/cli/blob/2f68ee4951ef96fa9d99e40c8ebadccf08412d58/libs/apps/runlocal/headers.go#L8-L17) | Local proxy behavior proves raw forwarded identity may be a username | Local simulated headers cannot enroll or authorize a deployed controlled action |
| [App secret resources](https://learn.microsoft.com/en-us/azure/databricks/dev-tools/databricks-apps/secrets) | Customer-controlled installation HMAC key can stay in a dedicated Databricks secret scope | App read permission is scope-wide; use a dedicated scope and never expose the secret in plain App environment settings |
| [Unity Catalog table resources for Apps](https://learn.microsoft.com/en-us/azure/databricks/dev-tools/databricks-apps/tables) | Narrow App bindings automatically grant the App principal parent use plus `SELECT` or `MODIFY` | The table/view must already exist; binding does not create it or grant collector, role, enrichment, or human-operator principals; `MODIFY` includes `SELECT` and views are select-only |
| [Delta Lake ACID guarantees](https://learn.microsoft.com/en-us/azure/databricks/lakehouse/acid) and [`MERGE`](https://learn.microsoft.com/en-us/azure/databricks/delta/merge) | One managed Delta statement/table provides an atomic commit and idempotent conditional insert/update path for runtime-trust events and the P6 singleton fence | By default each SQL statement is one transaction against one table and multi-table reads are snapshots; a trust read plus evidence/action write is not commit-current trust or a cross-table lock. Unity Catalog managed-Delta multi-table transactions require a Public Preview feature and are excluded |
| [Delta isolation levels](https://learn.microsoft.com/en-us/azure/databricks/optimizations/isolation/isolation-levels) | `Serializable` is the strongest GA table isolation and orders committed reads/writes against one table | P6 makes every claim/drain/phase/release/reopen writer modify the same verified singleton row. It still cannot make a later Jobs API call atomic, stop a malicious table owner/App, or coordinate a caller that bypasses dbtobsb |
| [Azure Databricks `CREATE TABLE [USING]`](https://learn.microsoft.com/en-us/azure/databricks/sql/language-manual/sql-ref-syntax-ddl-create-table-using), [`ALTER TABLE`](https://learn.microsoft.com/en-us/azure/databricks/sql/language-manual/sql-ref-syntax-ddl-alter-table), and [`ADD CONSTRAINT`](https://learn.microsoft.com/en-us/azure/databricks/sql/language-manual/sql-ref-syntax-ddl-alter-table-add-constraint) | Delta `CHECK` constraints are not valid inline `CREATE TABLE` constraints; add them afterward. String checks require `UTF8_BINARY` default collation | P6 uses one marked create, one isolation-property statement, eleven separately marked `ADD CONSTRAINT` statements, exact definition/property readback and partial-state recovery, then inserts the singleton last |
| [Azure Databricks `ALTER TABLE` required permissions](https://learn.microsoft.com/en-us/azure/databricks/sql/language-manual/sql-ref-syntax-ddl-alter-table#required-permissions) | `MODIFY` plus parent `USE` privileges can authorize many table alterations; ownership or `MANAGE` remains broader authority | Removing installer DML is necessary but not proof that an owner, `MANAGE`, or residual `MODIFY` principal cannot change evidence-bearing tables; preserve those roots in trust observations |
| [Primary and foreign key constraints](https://learn.microsoft.com/en-us/azure/databricks/tables/constraints) | Keys can document the trust-event schema | PK/FK constraints are informational, not enforcement; duplicate/conflicting rows must fail closed in the wrapper and sanitized view |
| [RFC 8785 JSON Canonicalization Scheme](https://www.rfc-editor.org/rfc/rfc8785) and [RFC 7493 I-JSON integer guidance](https://www.rfc-editor.org/rfc/rfc7493#section-2.2) | Stable UTF-8 canonical bytes for the literal-domain deployment, component, roster, stable-graph, phase-observation, event, candidate, acceptance, payload, snapshot, server-record, and row SHA-256 identifiers | JCS number serialization is binary64-based and I-JSON cannot promise exact integers beyond `9007199254740991`; keep Delta integral columns numeric but encode every canonical integral property as a range-checked unsigned decimal JSON string. Freeze every property, explicit null, array sort, ASCII/lexical restriction, dependency edge, and exclusion in shared Python/SQL/JavaScript vectors; never reinterpret a retained older contract |
| [`current_timestamp`](https://learn.microsoft.com/en-us/azure/databricks/sql/language-manual/functions/current_timestamp) | One statement-scoped `statement_evaluated_at` for each trust event | Databricks defines it at query-evaluation start, not Delta commit; the design exposes that fact and never labels it a commit timestamp |
| [Unity Catalog privilege reference](https://learn.microsoft.com/en-us/azure/databricks/data-governance/unity-catalog/access-control/privileges-reference) | Exact parent, `SELECT`, and `MODIFY` requirements for action tables | `MODIFY` also requires `SELECT`; enumerate each table rather than granting schema write |
| [Statement Execution API](https://learn.microsoft.com/en-us/azure/databricks/dev-tools/sql-execution-tutorial) | Fixed parameterized U2M statements; `wait_timeout` supports 5–50 seconds and `on_wait_timeout=CANCEL` requests cancellation | Only the executing user can fetch results; cancellation/timeout is not commit proof. Freeze one operation per request, `50s`, bounded client timeouts, no blind mutating retry, and reconstruct state after terminal Query History |
| [Query history UI](https://learn.microsoft.com/en-us/azure/databricks/sql/user/queries/query-history) and [GA Query History API](https://docs.databricks.com/api/azure/workspace/queryhistory/list) | A different approved actor can see full statement/user/details and cancel a running warehouse query; API supports warehouse/user/status/statement/time filters and pagination, defines the eight-state `QUEUED` through `FINISHED` vocabulary, and exposes a UUID query ID | Required only on a dedicated installer warehouse. Consume every documented status, require a canonical UUID before any cancellation handle, and keep malformed output indeterminate. Full text/user/error/profile exposure is accepted Personal Data/security metadata; do not copy unrelated history or depend on Preview `system.query.history`/query tags |
| [SQL warehouse ACLs](https://learn.microsoft.com/en-us/azure/databricks/security/auth/access-control/#sql-warehouse-acls) | Direct `CAN_MONITOR` combines query execution and warehouse-query visibility without stop/edit/permission management; workspace admins have effective `CAN_MANAGE` on workspace objects | Both groups receive direct `CAN_MONITOR`, but the actual workspace-admin verifier can also stop, edit, delete, and change warehouse ACLs. The separately named manager is the procedural escalation owner, not the exclusive technical manager |
| [`STATEMENT_TIMEOUT`](https://learn.microsoft.com/en-us/azure/databricks/sql/language-manual/parameters/statement_timeout) | Global/session timeout is not the recovery authority | Do not change a workspace-wide/default two-day setting as the product baseline; per-request cancel plus ten-minute escalation is bounded and local |
| [`GRANT`](https://learn.microsoft.com/en-us/azure/databricks/sql/language-manual/security-grant) and [`REVOKE`](https://learn.microsoft.com/en-us/azure/databricks/sql/language-manual/security-revoke) | Exact principal/privilege/object statements implement additive parent use and the temporary migration-operator-group ledger pair with targeted revoke | Never use `ALL PRIVILEGES` or broad reconciliation; a uniquely recoverable read-only Query History marker precedes each mutation |
| [Manage Unity Catalog privileges](https://learn.microsoft.com/en-us/azure/databricks/data-governance/unity-catalog/manage-privileges/) and [`SHOW GRANTS`](https://learn.microsoft.com/en-us/azure/databricks/sql/language-manual/security-show-grant) | Metastore admins, object/container owners, or object `MANAGE` principals can see all grants with `SHOW GRANTS`; ordinary users see only their own | Workspace-admin status alone is not complete UC grant visibility for an arbitrary customer catalog. `INFORMATION_SCHEMA` can omit grants even for `MANAGE`; the verifier needs explicit object-scoped authority and is a self-grant trusted root |
| [Get effective permissions API](https://docs.databricks.com/api/workspace/grants/geteffective) and [CLI 1.7.0 `grants get-effective`](https://github.com/databricks/cli/blob/2f68ee4951ef96fa9d99e40c8ebadccf08412d58/cmd/workspace/grants/grants.go#L139-L185) | Direct-plus-inherited permission inventory and page tokens feed live composite observation | Consume every page, including empty pages with a next token; incomplete visibility blocks. Direct-grant removal, current effective DML, and ownership/`MANAGE` self-grant capability remain distinct facts |
| [Create views](https://learn.microsoft.com/en-us/azure/databricks/views/create-views) | The attended envelope owns curated and sanitized attestation views | View creation requires schema `CREATE TABLE`; runtime App and deployment bindings remain select-only |
| [Unity Catalog ownership and `MANAGE`](https://learn.microsoft.com/en-us/azure/databricks/data-governance/unity-catalog/access-control/permissions-concepts) | Owners and `MANAGE` principals are trusted roots; `ALL PRIVILEGES` is dynamically implied and does not itself include `MANAGE` | Table ownership/current direct or inherited DML blocks; schema/catalog ownership or `MANAGE` without current DML is disclosed self-grant capability, not data access or permanent absence |
| [Databricks service principals](https://learn.microsoft.com/en-us/azure/databricks/admin/users-groups/service-principals) | Runtime collector, App, role, and optional enrichment identities remain separately governed | The regulated baseline has no migration service principal; runtime principals are bounded writers while their role managers and the dual-role verifier/account administrator remain named trusted roots |
| [Workspace access-control lists](https://learn.microsoft.com/en-us/azure/databricks/security/auth/access-control/) | Workspace admins have `CAN MANAGE` on all workspace objects and creators manage objects they create | A local handoff, direct warehouse ACL, or Job ACL cannot create separation from workspace administrators; record their effective authority or fail the customer's administrator-resistant requirement |
| [Restrict workspace admins](https://learn.microsoft.com/en-us/azure/databricks/admin/workspace-settings/restrict-workspace-admins) | Record the account-admin-controlled restriction on Job owner and run-as changes as defense in depth | It does not remove workspace admins' general object-management authority and cannot cure a mutable privileged Job design |
| [App best practices](https://learn.microsoft.com/en-us/azure/databricks/dev-tools/databricks-apps/best-practices) | App is stateless UI/API; offload SQL and batch work | Local disk and memory are ephemeral |
| [App dependencies](https://learn.microsoft.com/en-us/azure/databricks/dev-tools/databricks-apps/dependencies) | Reproducible Python packaging and lockfile | Installation egress must be explicit or dependencies mirrored |
| [Monitor Apps](https://learn.microsoft.com/en-us/azure/databricks/dev-tools/databricks-apps/monitor) | Native lifecycle, audit, log, and cost investigation | App logs are not the durable audit store |
| [App telemetry](https://learn.microsoft.com/en-us/azure/databricks/dev-tools/databricks-apps/observability) | Optional Databricks-native OpenTelemetry | Public Preview; disabled by default for v1 |
| [Official React plus FastAPI App template](https://github.com/databricks/app-templates/tree/main/nodejs-fastapi-hello-world-app) | Candidate UI/API foundation | Must be security-, accessibility-, and dependency-reviewed before adoption |
| [Databricks AppKit](https://github.com/databricks/appkit) | Current App development direction worth evaluating | `0.x`; not yet an accepted regulated-MVP dependency |
| [Serverless environment version 5](https://learn.microsoft.com/en-us/azure/databricks/release-notes/serverless/environment-version/five) | Candidate versioned environment with Python 3.12.3 | Pins the client/Python environment, not every rolling server implementation detail |
| [Configure the serverless environment](https://learn.microsoft.com/en-us/azure/databricks/compute/serverless/dependencies) | Native dbt task dependencies inherit the selected environment version | Pin the exact environment and complete dependency graph |
| [Serverless network policies](https://learn.microsoft.com/en-us/azure/databricks/security/network/serverless-network-security/network-policies) | Readiness and zero-runtime-egress policy | Availability and enforcement differ by workload and workspace configuration |
| [Manage serverless network policies](https://learn.microsoft.com/en-us/azure/databricks/security/network/serverless-network-security/manage-network-policies) | Installer preflight and controlled egress options | Never discover a locked-down incompatibility only after mutation |

## Jobs, artifacts, and governance

| Source | Decision supported | Current caution |
|---|---|---|
| [Run dbt transformations in Lakeflow Jobs](https://learn.microsoft.com/en-us/azure/databricks/jobs/how-to/use-dbt-in-workflows) | Native dbt task, injected run-as token, archived dbt outputs, task artifact link | Use the individual dbt task run ID; signed artifact links are temporary and must not be stored |
| [Pinned first-party Jobs run model](https://github.com/databricks/databricks-sdk-go/blob/bad015296869703de251d189b8a43ea4e6df8bfb/service/jobs/model.go#L3406-L3432) and [repair history](https://github.com/databricks/databricks-sdk-go/blob/bad015296869703de251d189b8a43ea4e6df8bfb/service/jobs/model.go#L4435-L4474) | A task run has its own run ID, and repaired runs retain repair history; the outer multi-task run alone is not the dbt attempt identity | Correlate an immutable outer-run/task-key/task-run-attempt tuple and fixture retries plus repairs; never overwrite the first attempt with the latest repair |
| [Pinned first-party dbt artifact-link model](https://github.com/databricks/databricks-sdk-go/blob/bad015296869703de251d189b8a43ea4e6df8bfb/service/jobs/model.go#L251-L259) and [CLI Jobs output-retention contract](https://github.com/databricks/cli/blob/09b5514a57ebe905755eec9eb27b2566c65f269b/cmd/workspace/jobs/jobs.go#L943-L970) | Native task output can expose a short-lived dbt artifact link, while Jobs run output is not a durable evidence store | Fetch promptly without persisting signed URLs or raw inline output. A missing/expired/error response remains `UNAVAILABLE` until an exact stable absent response and archive layout are qualified in staging |
| [Pinned Python SDK `DbtOutput`](https://github.com/databricks/databricks-sdk-py/blob/v0.117.0/databricks/sdk/service/jobs.py) | Documents the optional native dbt artifact output that the collector probes only as a compatibility path | Live Azure qualification did not return this output, so the supported path uses the product-owned runner and customer-local Unity Catalog staging Volume. |
| [Databricks SQL `read_files`](https://learn.microsoft.com/en-us/azure/databricks/sql/language-manual/functions/read_files) | Native SQL is suitable for post-acceptance analysis and ordinary read surfaces | It does not replace the strict tar, duplicate-key, immutable JSON Schema, and cross-file acceptance boundary |
| [Azure Databricks Default Storage](https://learn.microsoft.com/en-us/azure/databricks/storage/default-storage) | A serverless-created disposable catalog can store managed Delta tables and the managed raw Volume customer-locally | Serverless is required; availability, permissions, persistent storage cost, and production storage governance remain customer-specific |
| [Lakeflow Job privileges and Run as](https://learn.microsoft.com/en-us/azure/databricks/jobs/privileges) | Run-as is job-level; owners/`CAN_MANAGE` can edit tasks/configuration; only workspace admins transfer ownership | Runtime managers can change code that retains the configured service-principal authority, so they are evidence/action-state trusted roots. Drift detection is not malicious-admin custody |
| [Service principal roles](https://learn.microsoft.com/en-us/azure/databricks/security/auth/access-control/service-principal-acl) | Service Principal User permits running Jobs as the principal; Manager can change role assignments; roles are account-level; the native account-console Permissions page lists assignees | Job create/edit authority plus Service Principal User can bind code to that identity; SP Managers/account admins are trusted roots. In v1 the verifier must also be the account admin who compares the native roster and records a non-independent `SP_ROLE_ROSTER_ADMIN_ATTESTED` component |
| [Account Access Control Proxy API](https://docs.databricks.com/api/azure/workspace/accountaccesscontrolproxy) | Machine-readable service-principal role rule sets would support automation | Public Preview; prohibited in the required path. The administrator-attested native roster is point-in-time and never called API verification or continuous drift detection |
| [Run Job task](https://learn.microsoft.com/en-us/azure/databricks/jobs/tasks/run-job) | `ALL_DONE` task invokes the separately configured collector Job | Live test must prove the called Job uses its own run-as principal |
| [Jobs CLI commands](https://learn.microsoft.com/en-us/azure/databricks/dev-tools/cli/reference/jobs-commands), [Azure Jobs Run Now API](https://docs.databricks.com/api/azure/workspace/jobs/runnow), [Get Run 2.2](https://docs.databricks.com/api/azure/workspace/jobs/getrun), and the pinned first-party [`RunLifeCycleState`](https://github.com/databricks/databricks-sdk-go/blob/bad015296869703de251d189b8a43ea4e6df8bfb/service/jobs/model.go#L4129-L4177) | Run Now accepts an idempotency token; cancel is asynchronous; current `status` and deprecated `state` coexist; legacy `TERMINATED`, `SKIPPED`, and `INTERNAL_ERROR` are terminal; Get Run arrays paginate; runs/output are removed after 60 days | P6 stores one deterministic 64-hex token and retries only the identical request after response loss. Use current status/termination details first, legacy only as absent-current fallback, treat `INTERNAL_ERROR` as terminal failure, fail closed on conflict/unknown/deleted/incomplete evidence, and require final inventory before closure. No token or workload parameter enters dbt |
| [Bundle run-as identities](https://learn.microsoft.com/en-us/azure/databricks/dev-tools/bundles/run-as) | Declarative observed/collector service-principal separation | Generated grants must not let the observed principal write product evidence |
| [Run-if conditions](https://learn.microsoft.com/en-us/azure/databricks/jobs/run-if) | Collector uses `ALL_DONE` | Collector outcome must remain distinct from dbt outcome |
| [Azure Databricks dbt task](https://learn.microsoft.com/en-us/azure/databricks/jobs/tasks/dbt), [custom-profile workflow](https://learn.microsoft.com/en-us/azure/databricks/jobs/how-to/use-dbt-in-workflows#advanced-run-dbt-with-a-custom-profile), and [Bundle dbt task fields](https://learn.microsoft.com/en-us/azure/databricks/dev-tools/bundles/job-task-types) | The Job run-as principal receives an injected `DBT_ACCESS_TOKEN`; the documented custom profile uses literal Server Hostname and HTTP Path values; `profiles_directory` is allowed only when task `warehouse_id` is absent, and task catalog/schema are then absent too | v0.3.0 renders the canonical hostname from the authenticated named CLI profile and the HTTP path from the reviewed existing warehouse, makes `profiles_directory` equal the generated project directory, and asks the operator for no connection fields |
| [Dynamic value references](https://learn.microsoft.com/en-us/azure/databricks/jobs/dynamic-value-references) | Attempt correlation values passed without guessing | Values are text substitution; never expose free-form command input |
| [Jobs system tables](https://learn.microsoft.com/en-us/azure/databricks/admin/system-tables/jobs) | Optional dedicated enrichment Job can materialize selected Job/run facts from `system.lakeflow` | The schema covers the account's workspaces in the current cloud region and includes creator/run-as emails, service-principal IDs or groups, names, descriptions, tags, and Pipeline tables; fixed code immediately joins to the product-owned `(workspace_id, job_id)` scope and excludes non-allowlisted fields |
| [System-table scope and limitations](https://learn.microsoft.com/en-us/azure/databricks/admin/system-tables/) | Schema-wide grants and latency justify a separate paused enrichment capability | Exact table-only source grants are unavailable for this design; base install needs no `system` grant, and only the dedicated enrichment principal reads accepted schemas |
| [Billing system table](https://learn.microsoft.com/en-us/azure/databricks/admin/system-tables/billing) | Optional `dbt_job_cost_snapshot` can materialize allowlisted cost attribution from `system.billing` | Billing usage is account-global and routed to every region; coverage varies by compute and identity metadata can identify a run-as principal, so fixed code joins the pair scope before persistence and excludes raw identity/cross-workspace/unonboarded rows |
| [Monitor Lakeflow Job costs](https://learn.microsoft.com/en-us/azure/databricks/admin/system-tables/jobs-cost) | Direct versus allocated Job cost semantics | Shared SQL warehouse cost is estimated/allocated, never exact per run |
| [Audit system table](https://learn.microsoft.com/en-us/azure/databricks/admin/system-tables/audit-logs) | Optional native action enrichment | Public Preview; product action ledger is required without it |
| [Audit log reference](https://learn.microsoft.com/en-us/azure/databricks/admin/account-settings/audit-logs) | Native control-plane action evidence | Some audit-related system-table capabilities remain Preview; verify customer availability |
| [Unity Catalog Volume write limitation](https://learn.microsoft.com/en-us/azure/databricks/kb/dbfs/errno95-operation-not-supported) | Complete append-style files locally, then copy closed evidence | Do not stream active dbt logs directly to a Volume |
| [Automatic table retention](https://learn.microsoft.com/en-us/azure/databricks/tables/operations/auto-ttl) | Potential native retention option | Deletion is asynchronous and may not satisfy a hard deletion deadline without a verified workflow |

## dbt Core and adapter baseline

| Source | Decision supported | Current caution |
|---|---|---|
| [dbt-databricks 1.12.2](https://github.com/databricks/dbt-databricks/releases/tag/v1.12.2) | Candidate adapter pin and recent Python-model failure fix | Adapter 1.12.2 pairs with Core 1.11, not Core 1.12 |
| [dbt-databricks dependency constraints](https://github.com/databricks/dbt-databricks/blob/v1.12.2/pyproject.toml) | Candidate `dbt-core>=1.11.2,<1.11.13` range | Product pins the exact tested pair rather than this floating range |
| [dbt Core 1.11.12 Python metadata](https://github.com/dbt-labs/dbt-core/blob/v1.11.12/core/pyproject.toml) | Core Python floor and published classifiers | Candidate Python 3.12.3 is within the bounded 3.10-3.13 upstream intersection; full graph still requires a lock |
| [dbt-core 1.11.12](https://github.com/dbt-labs/dbt-core/releases/tag/v1.11.12) | Candidate Core pin | Must pass every golden-capture test before support is claimed |
| [dbt-adapters 1.23.0](https://pypi.org/project/dbt-adapters/1.23.0/) | First P1.1 candidate row whose raw Core 1.11.12 parsed manifest validates against the exact tagged schema without an overlay | Pin wheel SHA-256 `6c6c9cb6fca5d01324b5c39961c8b16b985b1ac7b701909f4416ce9dad66e288`; this row deliberately excludes later JavaScript-UDF support and remains a candidate until real runtime qualification |
| [dbt-adapters JavaScript UDF support PR](https://github.com/dbt-labs/dbt-adapters/pull/1932) | Explains why the separate 1.24.5 candidate can emit `javascript` in manifest macro languages under Core 1.11 | Do not silently overlay the schema; qualify that transitive dependency row separately |
| [dbt-adapters schema issue 1926](https://github.com/dbt-labs/dbt-adapters/issues/1926) | Records the Core-below-1.12 schema mutation needed by later JavaScript support | An import-time runtime mutation is not an immutable vendored schema contract |
| [dbt-spark 1.10.3](https://pypi.org/project/dbt-spark/1.10.3/) | Records an artifact-shaping transitive adapter dependency in the P1.1 candidate subgraph | Pin wheel SHA-256 `1906f4cb507c931c4988ea6074603769da7466788b53483546e3abf6eabbf04d`; the complete Linux lock is still required |
| [dbt-protos 1.0.541](https://pypi.org/project/dbt-protos/1.0.541/) | Records the exact candidate protocol dependency used by the local fixture source parse | Pin wheel SHA-256 `4e03f88e6b5c13a8cac68c6aa78267d8eb4396e6341a807112d619ab938318d5` inside the complete lock |
| [dbt-databricks 1.12.1](https://github.com/databricks/dbt-databricks/releases/tag/v1.12.1) | `job_id`, `job_run_id`, and `task_run_id` in `adapter_response` | Treat as corroboration; early failures may have no run-results artifact |
| [dbt-databricks 1.11.8](https://github.com/databricks/dbt-databricks/releases/tag/v1.11.8) | `invocation_id` in default query comments | Warehouse/query-history availability and retention vary |
| [dbt Core Databricks profile setup](https://docs.getdbt.com/docs/local/connect-data-platform/databricks-setup), [current Azure Databricks custom-profile workflow](https://learn.microsoft.com/en-us/azure/databricks/jobs/how-to/use-dbt-in-workflows#advanced-run-dbt-with-a-custom-profile), and [pinned adapter Jobs guide](https://github.com/databricks/dbt-databricks/blob/v1.12.2/docs/databricks-jobs.md) | A custom profile explicitly binds profile, target, adapter, host, HTTP path, catalog, schema, token source, and threads. The current serverless custom-profile workflow documents a literal host and HTTP path plus injected `DBT_ACCESS_TOKEN`; the pinned adapter guide's `DBT_HOST` example targets an interactive cluster and is not the v0.3 serverless contract | The product generates and hashes the profile from the authenticated workspace and reviewed warehouse; operators cannot supply a profile, target, token, host, path, or override |
| [dbt build](https://docs.getdbt.com/reference/commands/build) | One selector-scoped artifact-producing invocation | Downstream skips and test semantics must be represented correctly |
| [dbt warning configuration](https://docs.getdbt.com/reference/global-configs/warnings) | Promote the named zero-execution warnings and detect config conflicts | `WARN_ERROR` and `WARN_ERROR_OPTIONS` are mutually exclusive across configuration sources; fixture both `NoNodesForSelectionCriteria` and `NothingToDo` |
| [dbt artifacts](https://docs.getdbt.com/reference/artifacts/dbt-artifacts) | Artifact metadata, schema URLs, and invocation identity | Artifacts are sensitive and versioned contracts |
| [manifest.json](https://docs.getdbt.com/reference/artifacts/manifest-json) | Project inventory and Core 1.11 schema mapping | All enabled resources appear; presence does not mean execution |
| [run_results.json](https://docs.getdbt.com/reference/artifacts/run-results-json) | Primary node-level execution evidence | Not produced for every early failure |
| [Core 1.11.12 tagged manifest v12 schema](https://github.com/dbt-labs/dbt-core/blob/v1.11.12/schemas/dbt/manifest/v12.json) | Exact P1.1 manifest validation bytes | Vendor SHA-256 `b290bb419026be3fa361ead7f0d8e3bb0eb6bca8c3a3fb7971f5d252fd0825b3`; artifact metadata still contains the standard v12 URL |
| [Mutable manifest-v12 URL issue](https://github.com/dbt-labs/dbt-core/issues/12374) | Confirms that one schema URL can be updated in place | The generic URL was byte-identical to Core 1.11.7 and stale relative to Core 1.11.12 on 2026-07-15; never use it as an immutable supply-chain identity |
| [Core macro-config schema change](https://github.com/dbt-labs/dbt-core/commit/38d1483038fd729fbdf05ae31fdc1e0428f47e6c) | Explains the tagged schema difference required by Core 1.11.12 parsed macros | Use the complete tagged schema rather than a product-created partial patch |
| [Run-results v6 JSON schema](https://schemas.getdbt.com/dbt/run-results/v6.json) | Exact initial result validation contract | Validate the complete schema before applying product invariants |
| [sources.json](https://docs.getdbt.com/reference/artifacts/sources-json) | `source freshness` is a separate artifact/result type | Deferred in v1; never normalize it as build run results |
| [Core 1.11.12 BuildTask collections](https://github.com/dbt-labs/dbt-core/blob/v1.11.12/core/dbt/task/build.py) | Exact manifest collections against which build result IDs resolve | Do not assume every result belongs to `manifest.nodes` |
| [Core 1.11.12 global parameters](https://github.com/dbt-labs/dbt-core/blob/v1.11.12/core/dbt/cli/params.py) | Warning, usage, and artifact-ingest upload flags plus current/legacy aliases | An explicitly set `warn_error=false` still conflicts; generated commands disable artifact upload explicitly |
| [Core 1.11.12 flag precedence](https://github.com/dbt-labs/dbt-core/blob/v1.11.12/core/dbt/cli/flags.py) | Existing-job scanner must evaluate resolved CLI/environment/project/profile values | Searching command text alone can falsely classify a Job as ready |
| [Core 1.11.12 project/profile flag loading](https://github.com/dbt-labs/dbt-core/blob/v1.11.12/core/dbt/config/project.py) | Simultaneous non-empty project `flags` and profile `config` are a configuration error | They are not an ordered precedence chain, even when CLI overrides are present |
| [Core 1.11.12 command decorators](https://github.com/dbt-labs/dbt-core/blob/v1.11.12/core/dbt/cli/main.py) | `deps` accepts governed global flags but not selector or target-path | Generate and fixture separate exact argv templates for `deps` and `build` |
| [Core 1.11.12 log-version event](https://github.com/dbt-labs/dbt-core/blob/v1.11.12/core/dbt/cli/requires.py) | Persist `MainReportVersion.data.log_version` when logging initializes | Unknown versions quarantine event normalization without invalidating valid primary artifacts |
| [`dbt-common` 1.37.5](https://pypi.org/project/dbt-common/1.37.5/) | Exact candidate distribution has `LOG_VERSION=3` and five backups | Pin wheel SHA-256 `432e3f2f9ca61eace65d4d1cb99a715c3ffc343b7190a692c39f126eedf0c077` inside the complete Python 3.12/Linux lock |
| [Other dbt artifacts](https://docs.getdbt.com/reference/artifacts/other-artifacts) | Ancillary target outputs expected from normal parsing/build | Path/size check but do not normalize or persist them as primary evidence |
| [dbt events and logging](https://docs.getdbt.com/reference/events-logging) | Parse stable event fields rather than human message text | Event schemas and codes require compatibility fixtures |
| [dbt log configuration](https://docs.getdbt.com/reference/global-configs/logs) | JSON file logs, task-local path, rotation awareness | Debug logging is not the regulated default |
| [dbt JSON artifact configuration](https://docs.getdbt.com/reference/global-configs/json-artifacts) | Explicit JSON artifact generation and unique target paths | Multiple commands can overwrite the same target directory |
| [dbt environment variables](https://docs.getdbt.com/reference/dbt-jinja-functions/env_var) | Secret prefix behavior and custom-environment metadata | `DBT_ENV_CUSTOM_ENV_*` values are deliberately copied into evidence |
| [dbt usage statistics](https://docs.getdbt.com/reference/global-configs/usage-stats) | Disable anonymous dbt telemetry and interpret `DO_NOT_TRACK` | Truthy values are `1`, `t`, `true`, `y`, or `yes`, case-insensitive; generated commands still disable usage explicitly |
| [dbt retry](https://docs.getdbt.com/reference/commands/retry) | Explains why full Lakeflow-attempt retry is the product default | Retry depends on prior artifacts and conflicts with immutable attempt evidence |
| [dbt JSON schema registry](https://schemas.getdbt.com/) | Validate artifact schema versions before normalization | Unknown versions fail closed until fixture-tested |
| [dbt Core 1.12 release candidate](https://github.com/dbt-labs/dbt-core/releases/tag/v1.12.0rc3) | Trend watch for new statuses and behaviors | Not in the MVP support matrix |

## Optional intelligence and distribution

| Source | Decision supported | Current caution |
|---|---|---|
| [Genie Code workspace skills](https://learn.microsoft.com/en-us/azure/databricks/genie-code/skills) | Optional focused skill for authoring and migration | Agent mode only; guidance is not an enforcement boundary |
| [Genie Code instructions and `AGENTS.md`](https://learn.microsoft.com/en-us/azure/databricks/genie-code/instructions) | Workspace pointer and repository-level context | Does not cover every assistant feature and may be unavailable by policy |
| [Connect Genie Code to custom MCP tools](https://learn.microsoft.com/en-us/azure/databricks/genie-code/mcp) | Future narrow tool adapter hosted by the same App | Recent feature; regulated v1 cannot depend on it |
| [Genie Agents](https://learn.microsoft.com/en-us/azure/databricks/genie/) | Optional natural-language analysis over curated views | Read-only SQL; cannot run dbt or jobs |
| [June 2026 Azure Databricks release notes](https://learn.microsoft.com/en-us/azure/databricks/release-notes/product/2026/june) | Third-party Marketplace App installation direction | Provider packaging/review path still needs written confirmation |
| [Marketplace provider onboarding](https://learn.microsoft.com/en-us/azure/databricks/marketplace/become-provider) | Provider prerequisites and approval path | Marketplace does not block the private product |

## Documentation and usability

| Source | Decision supported | Current caution |
|---|---|---|
| [Diataxis](https://diataxis.fr/) | Four distinct documentation needs and structures | Apply as information architecture, not a prose style |
| [Diataxis compass](https://diataxis.fr/compass/) | Action/cognition and acquisition/application distinguish page modes | Classify by reader need, not by the information's subject |
| [Tutorials versus how-to guides](https://diataxis.fr/tutorials-how-to/) | Fixture learning and production bootstrap need distinct entry points | Keep the tutorial controlled and choice-free; real work belongs in a task guide |
| [Diataxis reference](https://diataxis.fr/reference/) | Neutral catalogues mirror the product machinery | Recovery actions link to how-to guides rather than living inside reference |
| [Diataxis complex hierarchies](https://diataxis.fr/complex-hierarchies/) | Audience/task landing pages and grouped navigation scale beyond flat lists | Preserve the four modes inside a user-recognizable hierarchy |
| [FastAPI Python types tutorial](https://fastapi.tiangolo.com/python-types/) | Progressive, concrete, approachable technical teaching | Use as a readability benchmark, not a theme copy |
| [FastAPI reference](https://fastapi.tiangolo.com/reference/fastapi/) | Precise reference organization | Keep task guides separate from reference |
| [Nielsen Norman usability heuristics](https://www.nngroup.com/articles/ten-usability-heuristics/) | Status visibility, user control, prevention, recognition, and recovery review | Supplement with real task walkthroughs |
| [NIST SP 800-53 Rev. 5 AC-5 and AC-6](https://csrc.nist.gov/pubs/sp/800/53/r5/upd1/final) | Separation of duties and least privilege support distinct actor/account boundaries for the regulated installer | The product contract is not a certification; customers still approve their own control implementation and role assignments |
| [WCAG 2.2 understanding documents](https://www.w3.org/WAI/WCAG22/Understanding/) | Labels, status messages, errors, focus, and keyboard review | Automated checks do not replace manual verification |
| [WCAG 2.2 Recommendation](https://www.w3.org/TR/WCAG22/) | Normative AA target for complete critical processes | Conformance applies to full pages/states, not isolated components |
| [WCAG accessible authentication](https://www.w3.org/WAI/WCAG22/Understanding/accessible-authentication-minimum.html) | Pairing must support paste/copy and avoid memorization/transcription barriers | QR can supplement but never replace the text path |
| [OWASP session management](https://cheatsheetseries.owasp.org/cheatsheets/Session_Management_Cheat_Sheet.html) | Secure host-only browser-profile credential controls | Treat the credential as a secret; server-side revocation must accompany cookie removal |
| [OWASP CSRF prevention](https://cheatsheetseries.owasp.org/cheatsheets/Cross-Site_Request_Forgery_Prevention_Cheat_Sheet.html) | Every state-changing App endpoint needs CSRF defenses | `HttpOnly` protects confidentiality but does not prevent CSRF |
| [RFC 9457 Problem Details](https://www.rfc-editor.org/rfc/rfc9457.html) | Safe, consistent API error representation | Keep long-running operation state separate from terminal problem details |
| [RFC 9110: 202 Accepted](https://www.rfc-editor.org/rfc/rfc9110.html#name-202-accepted) | Asynchronous acceptance and status-monitor semantics | A 202 response is not proof that the operation succeeded |
| [Azure REST API guidelines](https://github.com/microsoft/api-guidelines/blob/vNext/azure/Guidelines.md) | Pollable long-running operation and idempotency conventions | Product contract still excludes raw platform error content |
| [GOV.UK usability benchmarking](https://www.gov.uk/service-manual/measuring-success/usability-benchmarking-a-website-or-whole-service) | Representative-user measures and task baselines | Product thresholds are explicit internal gates, not universal standards |

## Refresh triggers

Refresh this register when:

- A supported dbt or adapter version changes.
- A Databricks Preview/Beta/Experimental feature becomes GA or changes limitations.
- The App template, AppKit, CLI, Bundle schema, or Marketplace process changes.
- Forwarded App headers, secret-resource scope, Bundle resource/binding/saved-plan behavior, App lifecycle, Job owner/run-as/admin controls, Service Principal roles, Jobs Run Now idempotency/cancel/Get Run behavior, Jobs output retention, Delta isolation/transaction status, Statement Execution result/timeout/cancel behavior, Query History permissions/API, SQL warehouse ACLs, or Unity Catalog grant-visibility/ownership semantics change.
- A customer has a compliance profile, region, or deletion requirement not represented here.
- A documentation claim is more than 30 days old at release time and describes pricing, availability, feature status, authentication, or permissions.
