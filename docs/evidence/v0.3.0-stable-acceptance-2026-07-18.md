# v0.3.0 stable Azure Databricks acceptance

- Date: 2026-07-18 UTC
- Environment: paid personal Azure Databricks workspace
- Data: synthetic weather operations project
- External telemetry: none
- Result: `PASS`

This record is sanitized. It omits workspace, Job, run, task, statement, principal, user, deployment, and Volume-locator identifiers. Raw dbt artifacts and logs remained inside the test workspace and were deleted through the supported destructive lifecycle.

## Qualification basis

The [2026-07-17 beta acceptance](v0.3.0-live-acceptance-2026-07-17.md) exercised the exhaustive live matrix: two deterministic complete runs, controlled dbt failure, deliberately missed collection, bounded reconciliation, direct SQL/App parity, retain-uninstall, delete-uninstall, and zero compute.

The stable source changed only product version identities, dependency constraints, and strict wheel-name acceptance from `0.3.0b1` to `0.3.0`. It did not change the support manifest, evidence contract, dbt policy, Job graph, DDL/DML, grants, collection logic, App query surface, or lifecycle behavior. The rebuilt stable artifacts independently passed the complete local suite and the live promotion path below.

## Stable artifact identity

The App, installer, capture library, collector, and contracts package all identified as `0.3.0`. The installer built and deployed three content-addressed final runtime wheels with this shape:

```text
dbtobsb_contracts-0.3.0+dbtobsb.final.<sha256>-py3-none-any.whl
dbtobsb_capture-0.3.0+dbtobsb.final.<sha256>-py3-none-any.whl
dbtobsb_collector-0.3.0+dbtobsb.final.<sha256>-py3-none-any.whl
```

The machine support manifest remained `FINAL` at digest `a440ab8a9cd6c0b00736e617554fd47d736b2764bdc3b17b71a935befc694401`.

## Live stable promotion path

The released launcher completed a fresh installation into an empty evidence schema. Its temporary serverless bootstrap Job succeeded, all nine product objects and fixed grants were verified, all three product Jobs were deployed, the App deployment exposed exactly four required bindings, and successful installation left the App stopped and reconciler paused.

One selector-bounded dbt build and its `ALL_DONE` collector both succeeded. The collector emitted:

```json
{"capture_state":"COMPLETE","event":"dbtobsb_collection_published","node_count":9,"pair_state":"PAIR_VALID","retrieval_state":"RETRIEVED"}
```

Independent Databricks SQL returned:

| Evidence | Stable result |
|---|---:|
| Run: `SUCCESS / RETRIEVED / COMPLETE / PAIR_VALID` | 1 |
| Node: model `success` | 3 |
| Node: seed `success` | 1 |
| Node: test `pass` | 5 |
| Collection: direct `PUBLISHED`, no issue | 1 |

The newly attested App returned HTTP 200 from health and readiness with version `0.3.0`. Its run, node, and collection APIs returned exactly 1, 9, and 1 rows, matching Databricks SQL. Every data response carried `X-DBTOBSB-Cost-Notice: query-may-auto-start-bound-sql-warehouse`. The launcher then stopped the App, and the warehouse returned to `STOPPED`.

The supported `dbtobsb uninstall --delete` path returned:

```json
{"app_state":"REMOVED","event":"dbtobsb_uninstall_verified","mode":"DELETE","product_objects":"REMOVED","schema_preserved":true}
```

## Final cleanup inventory

Authoritative paginated readback proved:

```text
active Job runs          0
classic clusters         0
SQL warehouse            STOPPED, 0 clusters, 0 active sessions
dbtobsb Jobs             0
Databricks App           absent
product tables/views     0
product Volumes          0
customer schema          preserved under its original owner
local installation state absent
```

No product schedule or paid compute was left running.

## Local and publication gates

`scripts/check_all.sh` returned `DBTOBSB_ALL_CHECKS_PASSED` for the stable artifacts. It included Ruff and Ty, installer and native Go suites, JavaScript semantic vectors, reproducible installer and runtime wheels, clean-wheel install/import probes, Bundle contract validation, 117 contract tests, 141 capture tests, 238 collector tests, 82 App tests, and the documentation link/fragment check.

The stable release remains private and customer-local. Marketplace distribution, separated duties, upgrade, rollback, external telemetry, and compliance certification remain outside v0.3.0.
